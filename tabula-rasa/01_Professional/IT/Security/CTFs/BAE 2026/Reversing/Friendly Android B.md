---
date created: Saturday, February 14th 2026, 7:20:44 pm
date modified: Saturday, February 14th 2026, 8:23:31 pm
---

# Friendly Android B

**Points:** 500pts
**Description:** This password file was created using the application in part A
**File:** `re006_75d6b19546501d936e29d6e0dbb95003.psafe3: Password Safe V3 database`
# Method:
## Description & Investigation:

The description hints at the program from [[Friendly Android A]] being used to create the password file. So looking back at the QR code and the rest of the code would be a smart idea.

Hint 1: Look for the key used in the "openNewFile" function.

Using this hint, we can find the area of the code, which is within the `openNewFile`:
```Java
fileData.createNewFile(new StringBuilder("Password is OK"), this);
```

The `passwd` parameter is just ignored and replaced with the hardcoded string. meaning that `Password is OK` will work for anything generated by the app. 

```Bash
pwsafe re006_75d6b19546501d936e29d6e0dbb95003.psafe3
```

Then you can open the `.pasfe3` vault and you get the key:
**I use the same password everywhere**

## Key Takeaways & Remediation:
- Make sure when searching to search for `password`, `key`, `secret` within the APK or file.